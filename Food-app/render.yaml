services:
  - type: web
    name: gofood-backend
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: cd Backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        value: mongodb+srv://vamsi:80p448cgte@cluster0.8tymqsr.mongodb.net/GoFood?retryWrites=true&w=majority
      - key: JWT_SECRET
        value: gofood_secret_key_2024_secure_jwt_token
      - key: PORT
        value: 10000

  - type: web
    name: gofood-frontend
    env: static
    plan: free
    rootDir: .
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: REACT_APP_API_URL
        value: https://gofood-backend.onrender.com
